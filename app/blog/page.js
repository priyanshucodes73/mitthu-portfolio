"use client";
import Section from "@/components/Section";

export default function BlogPage() {
  return (
    <Section title="Blog" subtitle="MDX-ready — hook up your content pipeline later.">
      <div className="space-y-4">
        <p className="text-slate-300">No posts yet — coming soon!</p>
      </div>
    </Section>
  );
}
